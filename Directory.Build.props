<Project>

  <PropertyGroup>
    <!-- packages.lock.json per project; CI restores in locked mode so the cache key is stable. -->
    <RestorePackagesWithLockFile>true</RestorePackagesWithLockFile>
  </PropertyGroup>

</Project>
